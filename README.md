First clone the repository and run the code in the notebook then run the make commands that are:
to build an image:
          make build
to run a container:
          make run
to delete an image:
          make rmi
